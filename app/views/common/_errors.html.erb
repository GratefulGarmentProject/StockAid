<div id="error_explanation">
  <p>
    <%= pluralize(record.errors.count, "error") %> prohibited this <%= record.class.name.downcase %> from being saved:
  </p>

  <ul>
    <% record.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
</div>
