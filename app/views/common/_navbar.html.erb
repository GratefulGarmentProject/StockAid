<nav class="navbar navbar-default">
  <div class="container-fluid">
    <ul class="nav navbar-nav">
      <li>
        <a href="/orders">Orders</a>
      </li>

      <li>
        <a href="/inventory">Inventory</a>
      </li>

      <% if current_user.can_create_organization? || current_user.organizations_with_admin_access.present? %>
        <li>
          <a href="/organizations">Organizations</a>
        </li>
      <% end %>

      <li>
        <a href="/users">Users</a>
      </li>
    </ul>

    <% if user_signed_in? %>
      <div class="pull-right">
        <div class="dropdown navbar-dropdown" style="margin-top: 10px">
          <button class="btn btn-default dropdown-toggle navbar-button" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
            <strong><%= current_user.email %></strong>
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
            <li><a href="<%= edit_user_path(current_user) %>">Edit User</a></li>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
          </ul>
        </div>
      </div>
    <% end %>
  </div>
</nav>
