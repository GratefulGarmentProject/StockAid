<% content_for :title, "Counties" %>

<% content_for :content do %>
  <div class="row">
    <div class="col-xs-12">
      <div class="pull-right">
        <button type="button" class="btn btn-default load-netsuite-assigned-counties">Fetch NetSuite Counties</button>
        <%= link_to "New County", new_county_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>

  <br />
  <div id="netsuite-assigned-counties-error"></div>

  <table class="table table-striped data-table">
    <thead>
      <tr>
        <th class="sort-asc">Name</th>
        <th>Allowed For</th>
        <th>External Id</th>
        <th>NetSuite County</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @counties.each do |county| %>
        <tr>
          <td><%= county.name %></td>
          <td><%= county.allowed_for_label %></td>
          <td><%= county.external_id %></td>
          <td class="netsuite-county" data-external-id="<%= county.external_id %>"></td>

          <td>
            <%= link_to edit_county_path(county), class: "btn btn-default btn-xs" do %>
              <i class="glyphicon glyphicon-pencil"></i>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
