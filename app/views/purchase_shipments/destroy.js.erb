$(".purchase-shipment-row[data-id='<%= @old_id %>']")
  .parents(".purchase-shipments-table")
  .find(".displayed-quantity-remaining")
  .html(<%= @purchase_detail.reload.quantity_remaining %>);
$(".purchase-shipment-row[data-id='<%= @old_id %>']").next("input[type='hidden']").remove();
$(".purchase-shipment-row[data-id='<%= @old_id %>']").remove();
