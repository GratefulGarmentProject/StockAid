# A blank server for the default site
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    server_tokens off;

    location / {
        add_header Content-Type text/plain;
        return 200 'Hello!';
    }
}
