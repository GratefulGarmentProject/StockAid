<% content_for :title, "Closed Orders" %>

<% content_for :content do %>
  <%= render partial: "orders/tabs" %>

  <div class="row">
    <div class="col-xs-12">
      <%= render partial: "orders/table", locals: { orders: @orders, for_closed: true } %>
    </div>
  </div>
<% end %>
