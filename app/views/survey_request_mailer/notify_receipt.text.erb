<%= @current_user.name %>,

You have successfully sent a survey notification with the following details:

Organizations (<%= @mail_attempts.size %>):
--------------
<%= @mail_attempts.map { |x| "* #{x.success ? "Success" : "FAILURE"} - #{x.org_request.organization.name} - #{x.org_request.organization.email}" }.join("\n") %>

Survey: <%= @survey_request.title %> - <%= survey_request_url(@survey_request) %>

Subject: <%= @subject %>

Body:
-----
<%= @body %>
